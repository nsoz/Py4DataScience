[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
authors = [
    {name="muoz", email="muoz@42.fr"}
]
description = "A sample test package"
readme = "README.md"
license = "MIT"